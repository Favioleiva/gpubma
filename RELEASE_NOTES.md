# 0.3.0rc1 final public candidate

Adds an exact p30 reference notebook that renders the accepted nine-figure suite from compact, verified full-enumeration artifacts without evaluating models. Includes exact top-five regression comparison and a separate true-model panel; correct two-panel frontiers, coefficient row order and Hamming-distance interpretation are preserved.

The existing gpubma package source and three BFG example notebooks are unchanged from the canonical reviewed candidate. Hard unique-model budget semantics and discovered-set/global probability distinctions are prominent. Documentation adds optional post-search shell recovery evidence and future small-shell hybrid ideas without installing a new inference API.

The package remains version 0.3.0rc1 pending human release approval. No remote push or release has been created. See TEST_REPORT.md, SCIENTIFIC_SCOPE.md, LIMITATIONS.md, EXAMPLE_INVENTORY.md, PUBLIC_EXCLUSIONS.md and SHA256_MANIFEST.json.
