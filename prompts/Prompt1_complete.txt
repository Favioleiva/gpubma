We need to add a new, harder synthetic benchmark to the GPUBMA repository and publish it to GitHub.

Repository root:

X:\Claude Agentic Tasks\GPU Powered BMA

Target directory:

data/synthetic/

IMPORTANT:

- Do not overwrite or modify the existing:
  - panel_8.*
  - panel_12.*
  - panel_30.*
- The current frozen panel_30 dataset must remain unchanged.
- This is a separate central-layer correlated-proxy benchmark.
- Preserve the existing public repository structure and file conventions.
- Do not rewrite Git history.
- Inspect the repository before making changes.

Create only the following dataset artifacts:

data/synthetic/panel_30_center15.parquet
data/synthetic/panel_30_center15_metadata.json

Also create a deterministic generator script in the repository’s appropriate source or scripts directory, following existing conventions.

Suggested name:

scripts/generate_panel_30_center15.py

If the repository already has a canonical location or generator framework for synthetic data, use that instead.

Do not create CSV or Stata .dta copies.

The canonical storage format for this new benchmark is Parquet.

Reasons:

- GPUBMA consumes the dataset in Python.
- Parquet preserves numeric types efficiently.
- Duplicate CSV and DTA artifacts would add storage and validation complexity without helping this benchmark.
- The metadata JSON must contain the complete structural truth, diagnostics, and SHA-256 hash of the Parquet artifact.

==================================================
1. PURPOSE OF THE NEW DGP
==================================================

The existing panel_30 artifact has a sparse true model of size 5 with independent predictors.

The new benchmark must test a substantially harder case:

- p = 30 candidate regressors
- true model size k_true = 15
- true model located in the central Pascal layer
- correlated true regressors
- 15 proxy regressors that compete with the 15 genuine regressors
- realized R² intentionally near 0.70
- exact exhaustive BMA still covers all:

  2^30 = 1,073,741,824 models

The true model lies in:

  C(30,15) = 155,117,520

This dataset is intended to test posterior recovery under collinearity and proxy substitution, not merely sparse variable selection.

==================================================
2. OBSERVATION STRUCTURE AND SCHEMA
==================================================

Use exactly 2,000 observations.

First inspect:

- data/synthetic/panel_30.parquet
- data/synthetic/panel_30_metadata.json
- notebooks/GPUBMA_A100_p30.ipynb

Also inspect any existing synthetic-data generator or documentation used for panel_8, panel_12, and panel_30.

Preserve schema compatibility wherever possible, including:

- outcome name
- candidate-variable naming
- always-included controls, if required by the current notebook/API
- identifier columns
- column ordering
- storage types

The candidate regressors must be named consistently as:

  x1, x2, ..., x30

or use the exact naming convention already established by the repository.

Do not introduce a conflicting naming system.

If the current canonical schema is a panel, construct a deterministic panel totaling exactly 2,000 rows while preserving compatibility.

Do not add unmodeled individual or time effects unless they are explicitly documented as part of this new DGP.

The central benchmark should isolate the challenge created by correlated predictors and proxies.

==================================================
3. TRUE REGRESSORS
==================================================

The genuine predictors are:

  x1–x15

Generate them from three latent-factor blocks:

- Block A: x1–x5
- Block B: x6–x10
- Block C: x11–x15

For each observation, generate three independent latent factors:

  F_A, F_B, F_C ~ N(0,1)

For each true regressor in its block, use a factor-loading construction such as:

  x_j_raw = loading_j * F_block
            + sqrt(1 - loading_j^2) * eta_j

where:

  eta_j ~ N(0,1)

Use deterministic heterogeneous loadings in a moderate-to-strong range, approximately 0.60–0.85.

Requirements:

- variables within each block must be meaningfully correlated;
- correlations across blocks should remain much smaller;
- the resulting design must remain full rank;
- all candidate regressors must subsequently be standardized using sample means and sample standard deviations.

Record the exact loading vector in metadata.

==================================================
4. PROXY REGRESSORS
==================================================

The proxy predictors are:

  x16–x30

Each proxy must be a noisy linear combination of one or more genuine predictors.

Use a deterministic one-to-one primary mapping:

  x16 ↔ x1
  x17 ↔ x2
  ...
  x30 ↔ x15

However, proxies must not be exact copies.

Each proxy should combine:

- a dominant loading on its corresponding true regressor;
- a smaller loading on another true regressor, preferably from the same block;
- independent Gaussian noise.

A representative structure is:

  proxy_raw_j =
      a_j * true_j
    + b_j * true_partner
    + sigma_proxy_j * nu_j

where:

  nu_j ~ N(0,1)

Use heterogeneous deterministic coefficients, with approximate ranges:

  a_j: 0.70–0.90
  b_j: 0.10–0.35

Choose proxy noise so that:

- each proxy is strongly but imperfectly correlated with its main true regressor;
- no proxy is an exact linear combination;
- the complete 30-column design remains full rank;
- the condition number remains numerically reasonable for float64 Cholesky enumeration.

Standardize all proxies after generation.

Record in metadata for every proxy:

- proxy variable name;
- primary true variable;
- secondary partner;
- primary coefficient;
- secondary coefficient;
- noise scale;
- realized correlation with the primary variable;
- realized correlation with the secondary variable.

==================================================
5. TRUE COEFFICIENT VECTOR
==================================================

Only x1–x15 belong to the true outcome equation.

x16–x30 must have structural coefficients exactly equal to zero.

Use a deterministic coefficient vector with:

- all 15 true coefficients nonzero;
- mixed positive and negative signs;
- heterogeneous magnitudes;
- no unrealistically tiny coefficient that becomes effectively undetectable at n = 2,000;
- no perfectly symmetric pattern that would make variables artificially interchangeable.

Use this starting vector:

  beta_true = [
      1.20, -1.10, 1.00, -0.90, 0.80,
      0.75, -0.70, 0.65, -0.60, 0.55,
      0.50, -0.45, 0.40, -0.35, 0.30
  ]

Use this vector unless repository compatibility or numerical diagnostics reveal a concrete problem.

Any change must be:

- explicitly justified;
- recorded in metadata;
- reported in the final summary.

The full structural beta vector is:

  beta = [beta_true, 0, ..., 0]

with exactly 15 trailing zeros.

Before generating the final artifact, print the proposed beta vector.

==================================================
6. ALWAYS-INCLUDED CONTROLS
==================================================

Inspect the current panel_30 dataset and A100 notebook.

If the canonical GPUBMA experiment requires always-included controls w1 and w2, retain them and generate them independently of the 30 candidate regressors:

  w1, w2 ~ N(0,1)

Use documented fixed coefficients compatible with the existing experiment, preferably:

  delta = (0.8, -0.6)

Do not allow w1 or w2 to become proxies for the candidate regressors.

If always-included controls are not required for the new dataset, explain that decision and preserve notebook compatibility through a separate configuration rather than silently changing API assumptions.

==================================================
7. OUTCOME AND R² CALIBRATION
==================================================

Generate:

  signal = X_true beta_true + W delta

and:

  y = signal + epsilon

with:

  epsilon ~ N(0, sigma²)

Calibrate sigma deterministically so that the realized in-sample R² of the true specification is approximately:

  R² ≈ 0.70

Required tolerance:

  0.695 <= realized R² <= 0.705

The R² must be computed from the actual frozen generated sample and the true specification, including:

- intercept;
- all always-included controls;
- x1–x15.

Do not obtain the target by repeatedly changing the random seed.

Use one fixed seed and calibrate the noise scale analytically or through a deterministic numerical adjustment.

Record:

- random seed;
- signal variance;
- target noise variance;
- realized noise variance;
- sigma;
- theoretical target R²;
- realized R²;
- exact variables included in the R² regression;
- exact formula or procedure used for calibration.

The true model must not have R² = 1.

==================================================
8. REPRODUCIBILITY
==================================================

Use a fixed, newly assigned seed clearly distinct from the existing dataset seeds.

The generator must:

- produce deterministic data;
- use a stable column ordering;
- use explicit dtypes;
- write only the Parquet dataset and metadata JSON;
- fail loudly if validation checks fail;
- avoid dependence on the current date;
- avoid dependence on machine state;
- avoid external services;
- avoid manually edited generated artifacts.

The Parquet file must be produced directly from the final validated in-memory dataframe.

Use deterministic Parquet-writing options where supported.

Document any library-level reason why the physical Parquet bytes may differ across different pyarrow or pandas versions even when the numerical dataset is identical.

==================================================
9. REQUIRED VALIDATION
==================================================

Before committing, validate and report:

A. Shape and schema

- exactly 2,000 rows;
- exactly 30 candidate regressors;
- expected controls and identifiers;
- no duplicate column names;
- no missing values;
- no infinite values;
- stable and documented column ordering;
- explicit numeric dtypes.

B. Structural truth

- exactly 15 nonzero candidate coefficients;
- true variables x1–x15;
- proxy/null variables x16–x30;
- no accidental proxy coefficient in the outcome equation;
- full beta vector contains exactly 15 nonzero values and 15 zeros.

C. Linear algebra

- full rank of the 30-column candidate design;
- full rank after residualizing on the always-included block;
- minimum eigenvalue of the residualized Gram matrix;
- maximum eigenvalue of the residualized Gram matrix;
- condition number of the residualized design or Gram matrix;
- successful float64 Cholesky for:
  - the true 15-variable model;
  - representative sparse models;
  - representative central-layer models;
  - representative dense models;
- no exact duplicate or numerically singular columns.

D. Correlation design

- within-block correlation summaries;
- across-block correlation summaries;
- true-variable/proxy correlations;
- strongest absolute pairwise correlations;
- minimum primary true–proxy correlation;
- maximum primary true–proxy correlation;
- confirmation that no proxy is an exact or near-exact duplicate;
- confirmation that correlations across latent blocks remain substantially smaller than within-block correlations.

E. Outcome

- realized R² within the required tolerance;
- response variance;
- signal variance;
- realized noise variance;
- sample mean and standard deviation of y;
- confirmation that the true model does not fit perfectly.

F. Parquet reload validation

After writing:

  data/synthetic/panel_30_center15.parquet

reload the Parquet file from disk and verify:

- exact row count;
- exact column count;
- exact column names;
- exact column ordering;
- expected dtypes;
- no missing or infinite values;
- numerical equality with the validated in-memory dataframe;
- rank diagnostics recomputed from the reloaded file;
- correlation diagnostics recomputed from the reloaded file;
- realized R² recomputed from the reloaded file;
- successful float64 Cholesky using data reloaded from Parquet.

The file written to disk, not only the in-memory dataframe, is the canonical dataset.

G. Checksums

Compute SHA-256 checksums for:

- generator script;
- Parquet artifact.

Record both hashes in the metadata JSON.

The metadata JSON cannot contain its own final SHA-256 hash without creating a self-reference.

After writing the final metadata JSON:

- compute its SHA-256 separately;
- report that metadata hash in the final completion report;
- do not rewrite the metadata file merely to insert its own hash.

==================================================
10. METADATA
==================================================

The metadata JSON must be detailed and machine-readable.

Include at least:

- dataset name;
- dataset version;
- purpose;
- distinction from the existing panel_30 benchmark;
- canonical data format: Parquet;
- generator path;
- random seed;
- number of observations;
- number of candidate regressors;
- true model size;
- true variable names;
- proxy variable names;
- complete 30-element structural beta vector;
- always-included controls and coefficients;
- latent-factor block definitions;
- factor loadings;
- proxy mappings;
- proxy coefficients;
- proxy noise scales;
- standardization procedure;
- outcome-generation equation;
- noise-calibration procedure;
- target R²;
- realized R²;
- signal and noise diagnostics;
- rank diagnostics;
- eigenvalue diagnostics;
- condition-number diagnostics;
- correlation diagnostics;
- output Parquet filename;
- Parquet SHA-256;
- generator SHA-256;
- creation environment;
- Python version;
- relevant package versions;
- statement that the true model lies in C(30,15);
- statement that the central layer contains 155,117,520 models;
- statement that proxies have zero structural coefficients;
- statement that the complete model space contains 2^30 models;
- statement that no CSV or DTA version is provided because Parquet is canonical.

Use stable JSON formatting and deterministic key ordering where practical.

==================================================
11. DOCUMENTATION
==================================================

Update the appropriate repository documentation so users understand that two different p = 30 benchmarks now exist:

1. panel_30

- existing frozen sparse benchmark;
- k_true = 5;
- independent candidate predictors;
- existing multi-format artifact remains unchanged.

2. panel_30_center15

- new correlated-proxy benchmark;
- k_true = 15;
- true model in the central Pascal layer;
- 15 genuine regressors;
- 15 structural-zero proxy regressors;
- canonical artifact available only as Parquet;
- detailed DGP stored in panel_30_center15_metadata.json.

Use the repository’s existing documentation location and style.

Do not rewrite unrelated documentation.

Do not yet change the canonical A100 notebook to use the new dataset unless a minimal compatibility test is necessary.

If a compatibility test is created, keep it separate and clearly labeled.

==================================================
12. TESTS
==================================================

Add focused tests covering:

- deterministic regeneration of the numerical dataframe;
- schema;
- column ordering;
- candidate-variable count;
- true coefficient count;
- structural-zero proxy coefficients;
- proxy mapping;
- R² tolerance;
- design rank;
- residualized rank;
- condition-number bounds;
- Parquet reload parity;
- metadata consistency;
- SHA-256 recording;
- protection against overwriting existing panel_30 files.

Do not add tests requiring CSV or DTA artifacts.

Do not weaken existing tests or numerical tolerances merely to obtain a passing result.

Run:

- the new focused tests;
- relevant existing synthetic-data tests;
- relevant enumeration or data-loading smoke tests;
- a minimal load and sufficient-statistics compatibility test using the new Parquet artifact.

Do not run a complete 2^30 enumeration locally merely to validate dataset creation.

==================================================
13. GIT AND GITHUB
==================================================

Before committing:

1. Show:

- current branch;
- git status;
- current HEAD;
- configured remote;
- whether local main differs from origin/main;
- whether public-main is used as an intermediate publishing branch.

2. Ensure existing unrelated working-tree changes are not accidentally included.

3. Stage only files related to this new synthetic benchmark, such as:

- data/synthetic/panel_30_center15.parquet
- data/synthetic/panel_30_center15_metadata.json
- generator script
- focused tests
- minimal documentation update

4. Create one focused commit with a clear message, for example:

  Add central-layer correlated-proxy p30 benchmark

5. Push the commit through the repository’s established safe public-publishing workflow.

The intended public dataset location is:

  data/synthetic/panel_30_center15.parquet

Do not force-push.
Do not rewrite public history.
Do not delete or replace existing artifacts.
Do not expose private files, prompts, logs, credentials, local paths, or unrelated working-tree content.

If the repository uses public-main as an intermediate publishing branch, inspect and follow that workflow.

Do not assume that local main can be pushed directly without checking.

After pushing, verify:

- remote branch;
- remote commit hash using git ls-remote;
- Parquet artifact is present in the pushed tree;
- metadata JSON is present in the pushed tree;
- generator is present in the pushed tree;
- tests and documentation changes are present;
- no CSV or DTA artifact was accidentally created or pushed;
- existing panel_30 files remain unchanged.

==================================================
14. FINAL REPORT
==================================================

At completion, report:

1. Files created.
2. Files modified.
3. Confirmation that only one canonical dataset file was created:
   - panel_30_center15.parquet
4. Exact beta vector.
5. Seed.
6. Dataset shape.
7. Column schema.
8. Realized R².
9. Rank and condition number.
10. Minimum and maximum Gram eigenvalues.
11. Main within-block correlation diagnostics.
12. Main across-block correlation diagnostics.
13. True-variable/proxy correlation range.
14. Strongest absolute pairwise correlations.
15. Parquet reload validation results.
16. Parquet SHA-256.
17. Generator SHA-256.
18. Metadata JSON SHA-256.
19. Tests executed and results.
20. Commit hash.
21. Branch pushed.
22. Remote verification.
23. GitHub paths for:
    - Parquet;
    - metadata JSON;
    - generator;
    - documentation.
24. Confirmation that panel_30 and all older datasets remain unchanged.
25. Confirmation that no CSV or DTA copy was created.
26. Confirmation that no unrelated files were committed.

Do not merely write code.

Generate, validate, document, test, commit, push, and verify the complete benchmark.